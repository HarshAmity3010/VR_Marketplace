{
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "name": "VR_MarketPlace",
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "prebuild": "npm run prebuild --workspaces --if-present",
    "pretest": "npm run prebuild --workspaces --if-present",
    "start": "npm start --workspaces --if-present",
    "test": "npm test --workspaces --if-present",
    "export:report": "node scripts/export-report.mjs",
    "export:docx": "node scripts/export-report-docx-native.mjs",
    "export:docx:native": "node scripts/export-report-docx-native.mjs",
    "export:diagrams": "node scripts/render-mermaid-kroki.mjs",
    "export:diagrams:local": "mmdc -i docs/figures/class_diagram.mmd -o docs/figures/class_diagram.png -b transparent && mmdc -i docs/figures/flow_login.mmd -o docs/figures/flow_login.png -b transparent && mmdc -i docs/figures/flow_purchase.mmd -o docs/figures/flow_purchase.png -b transparent"
  },
  "type": "module",
  "workspaces": [
    "src/VR_MarketPlace_frontend"
  ],
  "dependencies": {
    "@dfinity/auth-client": "^2.4.1"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "@types/testing-library__jest-dom": "^5.14.9",
    "marked": "^13.0.2",
    "html-to-docx": "^1.8.0",
    "docx": "^9.5.1",
    "@mermaid-js/mermaid-cli": "^11.12.0",
    "vite": "^7.0.0"
  }
}